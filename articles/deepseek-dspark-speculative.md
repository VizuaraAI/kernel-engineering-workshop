Everything on this site so far has been about making one kernel go faster. Speculative decoding is the first optimization on the ladder that is not a kernel at all — it is an *algorithm* for spending FLOPs you already have on tokens you might not keep. And yet, the moment you try to serve it, it collapses back into a pile of the most kernel-shaped problems you have ever seen: launch latency, occupancy, ragged batches, and a verification step that either coalesces or does not. `DeepSeek-V4-Pro-DSpark` is the cleanest recent example of this collapse, so it is what we will use to think it through.[[sn: DSpark is the V4-Pro checkpoint (1.6T total / 49B activated, 1M context) with a speculative-decoding module bolted on, published as an inference-optimized variant rather than a new base model. Everything here follows its model card; where the card is silent I say so.]]

The stakes are the usual autoregressive-decode stakes, only worse. A single forward pass through a 1.6-trillion-parameter model, even one that activates only **49 billion** of those parameters per token, produces exactly one token. Decode is memory- and latency-bound — you drag weights and KV cache across the memory system to emit one number, then do it again. Speculative decoding lets you emit several numbers per expensive pass, and whether it *actually* wins comes down entirely to how well the kernels underneath it are written.

## Why decode is the hard regime

From [the three regimes](the-three-regimes.html) we know a big training GEMM is compute-bound and lovely: arithmetic intensity in the thousands of FLOPs per byte, tensor cores lit. Autoregressive decode is the opposite animal. At batch size one you multiply the entire weight matrix by a single activation vector — a **general matrix-vector product** (GEMV), not a GEMM — with no reuse to amortize the weight load. You read `49B` parameters' worth of active experts, do a trickle of math, and emit one token. The `989 TFLOP/s` of BF16 tensor throughput on an H100 is almost entirely wasted; you are pinned against the `3.35 TB/s` of HBM3 instead.[[sn: This is why decode latency barely improves with a faster tensor core and improves a lot with faster memory or a smaller footprint — the exact reason FP4 experts and a 10%-sized KV cache matter so much for DSpark specifically.]]

Speculative decoding attacks this by changing the *shape* of the work. Instead of one token per expensive pass, you draft several cheap tokens, then verify all of them at once. Verification of `k` proposed tokens is a forward pass over a sequence of length `k` — a GEMM again, not a GEMV. You have converted a latency-bound GEMV into a small, throughput-friendly GEMM, and if the drafts are good you keep most of them. The whole game is: draft cheaply, verify in a batch, accept correctly.

[[fig: A hand-drawn Excalidraw pipeline timeline on pure white, fine black ink, all labels hand-lettered Virgil-style. Black title "Speculative decode: one target pass, many tokens". Three wobbly rounded-rectangle lanes stacked vertically, each with a black lane label at the left. Top lane labeled "DRAFT": a small blue-hatch box "draft · MTP head" on the left with a thin blue curved dashed arrow to a chain of 7 tiny hand-drawn token circles numbered (1)..(7) left to right; a blue handwritten note "cheap, sequential, greedy — 7 launches" joined by a dashed arrow to the chain. Middle lane labeled "VERIFY": one wide green-hatch box "TARGET forward pass — seq len k=7" spanning the full width directly under all 7 tokens, with a green handwritten note "GEMM shape, tensor-core friendly, ONE launch" dashed-arrowed to it. Bottom lane labeled "ACCEPT": the same 7 tokens redrawn, the first 4 filled pale-green (kept) and the last 3 struck through in red, an orange handwritten callout "longest matching prefix kept + 1 bonus token" pointing at the boundary. A red dimension double-arrow (↔) under the accepted run labeled in red "≈ n+1 tokens / target pass". A dashed rounded takeaway box bottom-right reads in black "GEMV → GEMM: the whole trick". Flat, no shadows, no gradients, generous white space. || The three stages. Drafting is sequential and cheap; verification is one batched target pass; acceptance decides how much you keep.]]

## Stage one: the draft forward passes

The draft has to be cheap or the whole scheme is pointless — every FLOP spent drafting a token you later reject is pure loss. DSpark's vLLM recipe configures the speculative module for `num_speculative_tokens: 7` with `draft_sample_method: "greedy"`. Seven proposed tokens per target pass is aggressive; it only pays off because the drafter is tightly coupled to the target and the acceptance rate stays high.

The kernel tension here is subtle. Drafting is *inherently sequential* — token `i+1` depends on token `i` — so you cannot batch the seven draft steps into one GEMM the way you can with verification. Each draft step is its own tiny, latency-bound launch: the **overhead-bound** regime, where the work per step is so small that kernel-launch latency and the fixed cost of touching the GPU dominate. A single-layer **Multi-Token Prediction** (MTP) head, trained alongside the target, is the standard DeepSeek-family drafter — it reuses the target's own hidden state, so a draft step is close to one extra transformer layer rather than a whole separate model forward. The fix is the overhead-regime fix: fuse the launches. Full-graph capture with CUDA graphs — which DSpark's config enables — replays the entire seven-step draft chain as one submitted graph, so you pay launch latency once instead of seven times.

Conceptually the draft loop is nothing more than:

```python
# draft: sequential, cheap, greedy — captured as ONE cuda graph
draft_tokens = []
h = target_hidden_state          # reuse the target's last hidden state
for _ in range(num_speculative_tokens):   # 7
    logits = mtp_head(h)         # ~one transformer layer of work
    tok = logits.argmax(-1)      # greedy: no sampling kernel needed
    draft_tokens.append(tok)
    h = mtp_head.step(h, tok)    # advance the draft state
```

The `argmax` matters more than it looks. Greedy drafting means the draft path needs *no* sampling kernel — no Gumbel noise, no top-p sort, no CDF walk. Each of those is an extra launch and an extra dependency in the graph, and skipping them on the hot draft path is why greedy is the default here rather than a compromise.

## Stage two: parallel verification

Now the good part. You hand the target model the current sequence plus all seven drafted tokens and ask it to produce, in a single forward pass, the distribution it *would* have produced at each of those positions. Because you already have all the input tokens in hand, there is no sequential dependency — you process positions `1..k` in parallel. That is a GEMM of sequence length `k`, which is precisely the shape the tensor cores want.

The subtlety that trips people up is the attention mask. Position `i` may only attend to the real context plus the drafted tokens strictly before it, so verification runs a standard causal mask over the drafted block and every position gets the target's *honest* next-token distribution as if that prefix were real. Get the mask wrong and you leak future draft tokens backward, the acceptance test silently passes garbage, and your outputs diverge from plain autoregressive decode in ways that are miserable to debug.

For DSpark this verification pass is where all the model's architectural frugality earns its keep. The target attention is the hybrid **Compressed Sparse Attention** (CSA) plus **Heavily Compressed Attention** (HCA) scheme, which at a 1M-token context needs only **27% of the single-token inference FLOPs** of DeepSeek-V3.2 and, just as importantly, **10% of the KV cache**.[[sn: The FLOP and cache reductions are the model card's figures at 1M context, measured against V3.2; at shorter contexts the sparse-attention advantage shrinks because the dense-attention baseline was never the bottleneck there.]] The KV number is the one that matters for spec-dec: verification reads the entire KV cache, so a cache one-tenth the size is one-tenth the bytes to drag across HBM3 on every target pass. A memory-bound step over a cache you shrank by 10× finishes roughly 10× sooner.

[[fig: A hand-drawn Excalidraw memory-and-FLOPs comparison on pure white, fine black ink, hand-lettered Virgil-style labels. Black title "Why the target pass is affordable". Left half: a tall wobbly bar labeled in black "KV cache @ 1M ctx" — a full-height grey bar annotated in red "V3.2 baseline = 100%", and a short slice at the very bottom filled with green diagonal hatch labeled in red "DSpark CSA+HCA = 10%", with a green handwritten note "10× fewer bytes to read on verify" dashed-arrowed to it. Right half: two side-by-side FLOP bars — a tall grey one labeled in red "V3.2 single-token FLOPs = 100%" and a short green-hatch one labeled in red "DSpark = 27%", with an orange handwritten callout "sparse attention, not dense" pointing between them. A blue curved dashed arrow runs from the shrunken KV bar to a small black box labeled "TARGET verify pass", carrying a blue handwritten note "verification reads the WHOLE cache every pass — shrinking it is the win". A purple handwritten config note bottom-left reads "fp8 kv-cache · block_size 256 · expert parallelism". A dashed rounded takeaway box bottom-right reads in black "cheaper verify → speculation pays off sooner". Flat, no shadows, no gradients, generous white space. || The sparse-attention budget. A 10%-sized, FP8 KV cache is what makes the batched verify pass cheap enough for speculation to net out positive.]]

## Stage three: the acceptance-sampling kernel

This is the step that is easy to get subtly, dangerously wrong, and it is a real kernel. After verification you have, for each of the seven positions, the drafter's proposed token and the target's true distribution at that position. You walk the draft left to right and accept token `i` only if it survives the acceptance test against the target distribution; the first rejection truncates the run, and you append one *bonus* token sampled from the corrected residual distribution at the rejection point. The guarantee that makes speculative decoding respectable is that this procedure produces a sequence *distributed identically* to plain autoregressive sampling from the target — you get exactly the target model's outputs, only faster.[[sn: For greedy drafting the accept test collapses to a plain equality check — accept the draft token iff it equals the target's argmax — which is why the DSpark recipe pairs `draft_sample_method: "greedy"` with the simplest possible acceptance kernel. Full speculative sampling with a temperature needs the residual-distribution correction; greedy does not.]]

As a kernel this wants to be one fused pass over the `k` positions that does the comparison, finds the longest accepted prefix, and emits the bonus token, all in registers, with one launch per sequence. The naive version — a separate launch for the compare, another to prefix-scan the accept mask, another to sample the bonus token — is a textbook overhead-bound mistake: three tiny launches dominated entirely by launch latency. Fusing them into one kernel is the difference between acceptance sampling being free and it eating your speculative savings alive.

```cpp
// per sequence in the batch: fuse compare + prefix-scan + bonus sample.
// greedy draft ⇒ accept iff draft token == target argmax at that position.
__global__ void accept_greedy(const int* draft, const int* target_argmax,
                              int* out_tokens, int* out_len, int k) {
    // one block per sequence; threads cooperate over the k positions
    int n = 0;
    while (n < k && draft[n] == target_argmax[n]) ++n;   // longest match
    for (int i = 0; i < n; ++i) out_tokens[i] = draft[i];
    out_tokens[n] = target_argmax[n];                     // +1 bonus token
    *out_len = n + 1;
}
```

The shape of the win falls straight out of this: if you accept `n` of the drafted tokens, you have produced `n + 1` real tokens from a single expensive target pass. With a well-matched MTP drafter, acceptance runs high enough that the *effective* tokens-per-pass climbs to several, and since the target pass was the only expensive thing you did, your decode latency drops by roughly that same factor.

## Stage four: batching, where it all interacts

None of the above lives in isolation on a real server. DSpark's reference deployment is `deep_gemm_mega_moe` on a **single 4×GB300 node** — one node holding four GB300 GPUs — with expert parallelism (`data_parallel_size: 4`), an FP8 KV cache at `block_size: 256`, and full CUDA-graph compilation. Every one of those choices exists to keep the pipeline from stalling, and they interact in ways that are pure kernel engineering.

The hard interaction is that speculation makes batches *ragged*. Every sequence accepts a different number of tokens on each step — one keeps six, its neighbor keeps two — so after the acceptance kernel the sequences are at different lengths and want different amounts of work on the next draft. A **Mixture-of-Experts** (MoE) model makes this worse: with `1.6T` total parameters routed down to `49B` active, each token in the verification batch selects a different subset of experts, so the per-expert GEMMs are themselves ragged. This is exactly what a *grouped* / mega GEMM backend is for — `deep_gemm_mega_moe` packs the variable-sized per-expert multiplies into one launch and keeps the tensor cores fed instead of draining and refilling between experts. With experts split across the four GB300s, routing also incurs an all-to-all exchange to ship each token's activations to whichever GPU holds its experts and back.[[sn: The FP4 experts (`e2m1`-style 4-bit weights) plus FP8 for the rest of the model exist for the same reason as the shrunken KV cache: fewer bytes per active parameter moved across HBM per token, which is the binding constraint in decode. Halving the expert footprint roughly halves the dominant memory traffic of the verify pass.]]

Then the whole thing is captured as a CUDA graph. Because speculation multiplies the number of distinct small launches — draft steps, verify, accept, bonus sample, the cross-GPU all-to-all for expert parallelism — the fixed launch cost is a first-order term, not a rounding error. Full-graph capture replays the entire draft-verify-accept cycle as one submitted graph per step: the difference between the GB300 tensor cores being busy and them waiting on the launch queue. It is the overhead-regime playbook from [the three regimes](the-three-regimes.html), applied to an entire decoding step rather than one kernel.

[[fig: A hand-drawn Excalidraw architecture map on pure white, titled in black "DSpark serving on one 4×GB300 node". A large black outer dashed rounded box labeled in black "single node · expert-parallel, data_parallel_size 4" contains four wobbly rounded rectangles each labeled in black "GB300 GPU", arranged in a 2×2 grid and connected by orange double-headed curved arrows labeled in orange "all-to-all (expert routing)". One GPU box is enlarged and holds a vertical stack of hatched boxes: a blue-hatch box labeled in black "draft: MTP head — 7 toks" with a blue handwritten note "sequential, greedy", a green-hatch box labeled in black "verify: target GEMM, seq=7", and a pale-yellow-hatch box labeled in black "accept kernel". To the right of that stack, a tall black-outlined box labeled "deep_gemm_mega_moe" with a purple handwritten note "grouped per-expert GEMMs, FP4 experts, packed into ONE launch". Down the left margin, green handwritten spec notes each joined by a thin dashed curved arrow to the node box: "1.6T total / 49B active", "FP4 experts + FP8 elsewhere", "FP8 KV cache, block_size 256". A blue dashed arrow wraps the enlarged GPU's stack, labeled in blue "captured as ONE cuda graph → launch cost paid once". Hand-drawn red numbered circles (1) at draft (2) at verify (3) at accept trace the loop order. A dashed rounded takeaway box bottom-right reads in black "ragged batches + MoE ⇒ grouped GEMM + graph capture". Flat, no shadows, no gradients, generous white space. || The full serving picture. Speculation, MoE routing, and expert parallelism all pile ragged small launches onto four GPUs in one node; the mega-MoE backend and graph capture are what keep the tensor cores fed.]]

## Where this leaves us

Step back and the pattern is the same one the GEMM ladder taught us, only larger. We started with a latency-bound GEMV, formed a hypothesis — draft cheaply, verify in a batch — that reshapes it into a throughput-friendly GEMM, and then found that every practical win came from the same three levers we have pulled since kernel 1: coalesce the memory traffic (a `10%` KV cache, FP4 experts, FP8 everywhere else), fuse the launches (greedy drafting, a single acceptance kernel, full-graph capture), and keep the tensor cores fed across ragged work (grouped mega-MoE GEMMs). Speculative decoding is not an escape from kernel engineering. It is kernel engineering with a wider aperture — the same predict-the-regime, measure, fuse loop, now spanning a four-GPU node instead of a single [SM](shared-memory-l1.html).

The honest caveat to close on: speculation only nets out positive when the acceptance rate is high enough that the effective tokens-per-pass exceeds the overhead you added. A badly matched drafter, or a verify pass no cheaper than a plain decode step, and you have built an elaborate machine that runs slower than the naive loop. DSpark is a real point where the arithmetic works — a `10%` KV cache and FP4 experts make the verify pass genuinely cheap, and a trained MTP drafter keeps acceptance high — which is why it is worth studying rather than admiring from a distance.
